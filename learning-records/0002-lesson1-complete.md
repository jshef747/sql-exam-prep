# Lesson 1 (select–from–where + distinct) completed

User reported completing Lesson 1. Floor is now: single-table `select`/`from`/`where`, `distinct`, `select *`, `and/or`, and the π/σ/× mental model. No misconceptions surfaced yet (no SQL written back to me — evidence is self-reported completion, not demonstrated).

Next in ZPD: **joins** (Lesson 2) — combining rows across tables, the single biggest leap and a prerequisite for almost every Part-B exam question. After joins: `LIKE`, then aggregation. See [[MISSION.md]] and `NOTES.md` for the full arc.
